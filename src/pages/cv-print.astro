---
const cvData = {
    personalInfo: {
        name: `Jonathan GATARD`,
        title: `Ingénieur Mainframe & Interopérabilité`,
        email: `jonathan.gatard@gmail.com`,
        phone: `06 13 54 83 19`,
        website: `jonathan-gatard.fr`,
        linkedin: `linkedin.com/in/jonathan-gatard`,
        misc: `Permis B — Véhiculé`,
        photo: `/images/profil.png`
    },
    skills: {
        mainframe: [`z/OS`, `USS`, `JCL`, `Cobol`, `CICS`, `DB2`, `Zowe`, `Rexx`, `VSCode`, `TSO`],
        open: [`Python`, `React`, `NodeJS`, `Ansible`, `Docker`, `CI/CD`, `Linux`, `Git`, `Grafana`, `API REST`],
        methodology: [`Agile/Scrum`, `DevOps`, `AppOps`, `ITIL 4`, `Lean Management`, `amélioration continue`]
    },
    languages: [
        { name: `Français`, level: `Natif`, pct: 100 },
        { name: `Anglais`, level: `Courant (C1)`, pct: 85 },
        { name: `Espagnol`, level: `Scolaire`, pct: 35 },
        { name: `Slovaque`, level: `Notions`, pct: 15 },
    ],
    experiences: [
        {
            role: `Ingénieur Mainframe & Interopérabilité`,
            company: `Solutec & CAGIP (Rodez)`,
            date: `Septembre 2023 – Présent`,
            details: [
                `MCO & Expertise z/OS : Maîtrise USS (Z Linux), astreintes (jobs plantés, stockage) et diagnostic.`,
                `Modernisation : Pilote Zowe/VSCode, appels API vers servitudes techniques sur VMs et automatisation CATS.`,
                `Projets Squads : Développement d'outils de gestion de logs et playbooks Ansible pour Cobol.`,
                `AppOps : Pont technique entre besoins applicatifs et exigences opérationnelles mainframe.`
            ]
        },
        {
            role: `Manager Projets & Ingénieur DevOps`,
            company: `Stellantis (Slovaquie)`,
            date: `2021 – 2023`,
            details: [
                `Management : Encadrement d'une équipe de 4 personnes et pilotage de projets IT industriels.`,
                `Développement & Automatisation : Fullstack (React/NodeJS), CI/CD et monitoring (Grafana, Node-Red).`,
                `Data Management : Reporting PowerBI et collecte de données industrielles.`
            ]
        },
        {
            role: `Ingénieur Projets Logistique & Amélioration Continue`,
            company: `LISI Aerospace (Alternance & CDI)`,
            date: `2016 – 2021`,
            details: [
                `Logistique Industrielle : Optimisation de flux et implantation de zones pour usines neuves.`,
                `Lean Management : Déploiement d'outils Lean (5S, VSM, PDCA) et amélioration satisfaction client.`,
                `Software : Développement d'outils de gestion internes en VB.NET.`
            ]
        }
    ],
    education: [
        {
            degree: `Sécurité & Réseaux (Autodidacte)`,
            school: `TU Graz (Autriche)`,
            date: `2022`,
            details: [`Sécurité réseau, CTF, Intelligence Artificielle et Réalité Augmentée.`]
        },
        {
            degree: `Ingénieur Aéronautique & Spatial (Alternance)`,
            school: `CNAM / ISAE-Supaero / ISAE-ENSMA`,
            date: `2016 – 2019`,
            details: [`Thermodynamique, matériaux composites et mécanique des fluides.`]
        },
        {
            degree: `DUT Science et Génie des Matériaux`,
            school: `IUT Nantes`,
            date: `2014 – 2016`,
            details: [`Étude des métaux, polymères, composites et caractérisation mécanique.`]
        }
    ],
    interests: [
        { icon: `🏠`, text: `Domotique complète (Home Assistant, Docker, ZigBee, 25+ conteneurs)` },
        { icon: `🌐`, text: `Infrastructure Réseau (OpenWrt, VPN, Reverse Proxy, Frigate)` },
        { icon: `✈️`, text: `Aéromodélisme (construction, pilotage, firmware OpenTX)` },
        { icon: `🔧`, text: `Mécanique auto (turbo, swap moteur, diagnostic ISTA)` },
        { icon: `🖨️`, text: `Impression 3D & Laser (Prusa DIY, Voron CoreXY, Klipper)` },
        { icon: `🌿`, text: `Terrariophilie high-tech & Potager intelligent` },
    ]
};

const { personalInfo, skills, languages, experiences, education, interests } = cvData;
---

<html lang="fr">
<head>
    <meta charset="utf-8" />
    <title>CV — jonathan GATARD</title>
    <meta name="viewport" content="width=device-width" />
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap');
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        @page {
            size: A4;
            margin: 0;
        }
        @media print {
            html, body { 
                width: 210mm;
                height: 297mm;
                -webkit-print-color-adjust: exact; 
                print-color-adjust: exact;
            }
            .no-print { display: none !important; }
            .page { box-shadow: none !important; }
        }

        body {
            font-family: 'Inter', system-ui, sans-serif;
            background: #0a0a0f;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 2rem 0;
        }
        @media print { body { padding: 0; background: white; display: block; } }
        
        .page {
            width: 210mm;
            height: 297mm;
            background: white;
            display: flex;
            overflow: hidden;
            box-shadow: 0 25px 50px rgba(0,0,0,0.5);
            position: relative;
        }

        /* ── Left sidebar ── */
        .sidebar {
            width: 72mm;
            background: #111827;
            color: white;
            padding: 5mm 5mm 3mm;
            display: flex;
            flex-direction: column;
            gap: 2.5mm;
            flex-shrink: 0;
        }
        
        .photo-wrapper {
            display: flex;
            justify-content: center;
            margin-bottom: 1mm;
        }
        .photo {
            width: 28mm;
            height: 28mm;
            border-radius: 50%;
            border: 3px solid #3b82f6;
            object-fit: cover;
            box-shadow: 0 0 0 3px rgba(59,130,246,0.2);
        }

        .sidebar-name {
            text-align: center;
            font-size: 16pt;
            font-weight: 800;
            letter-spacing: 0.5px;
            line-height: 1.2;
        }
        .sidebar-title {
            text-align: center;
            font-size: 8.5pt;
            color: #60a5fa;
            font-weight: 600;
            margin-bottom: 2mm;
        }
        
        .section-title {
            font-size: 7.5pt;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #6b7280;
            border-bottom: 1px solid #1f2937;
            padding-bottom: 1.5mm;
            margin-bottom: 2mm;
        }

        .contact-list {
            list-style: none;
            font-size: 8.5pt;
            color: #d1d5db;
        }
        .contact-list li {
            display: flex;
            align-items: center;
            gap: 2mm;
            margin-bottom: 2mm;
        }
        .contact-list .icon {
            width: 5mm;
            text-align: center;
            font-size: 9pt;
        }

        .skill-group h3 {
            font-size: 9pt;
            font-weight: 700;
            color: white;
            margin-bottom: 1.5mm;
        }
        .skill-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 1mm;
            margin-bottom: 3mm;
        }
        .skill-tag {
            font-size: 7.5pt;
            padding: 1mm 3mm;
            background: #1f2937;
            border: 0.5px solid #374151;
            border-radius: 2mm;
            color: #d1d5db;
        }

        .lang-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 8.5pt;
            color: #d1d5db;
            margin-bottom: 2mm;
        }
        .lang-bar-bg {
            width: 20mm;
            height: 2mm;
            background: #1f2937;
            border-radius: 1mm;
            overflow: hidden;
        }
        .lang-bar-fill {
            height: 100%;
            border-radius: 1mm;
            background: linear-gradient(90deg, #3b82f6, #8b5cf6);
        }

        /* ── Right main content ── */
        .main {
            flex: 1;
            padding: 5mm 8mm 4mm 8mm;
            display: flex;
            flex-direction: column;
            gap: 2.5mm;
            overflow: hidden;
        }

        .summary-box {
            background: #eff6ff;
            border-left: 3px solid #3b82f6;
            padding: 3mm 4mm;
            font-size: 8.5pt;
            font-style: italic;
            color: #374151;
            line-height: 1.4;
            border-radius: 0 2mm 2mm 0;
        }

        .main-section-title {
            font-size: 11pt;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #111827;
            border-bottom: 2px solid #e5e7eb;
            padding-bottom: 1.5mm;
            display: flex;
            align-items: center;
            gap: 2mm;
        }
        .main-section-title .accent {
            width: 3.5mm;
            height: 3.5mm;
            background: #3b82f6;
            border-radius: 0.5mm;
            flex-shrink: 0;
        }

        /* Experience cards */
        .exp-card { margin-bottom: 2mm; }
        .exp-header {
            display: flex;
            justify-content: space-between;
            align-items: baseline;
            margin-bottom: 1mm;
        }
        .exp-role {
            font-size: 11pt;
            font-weight: 700;
            color: #111827;
        }
        .exp-date, .edu-date {
            font-size: 7.2pt;
            font-weight: 600;
            color: #4b5563;
            background: #f3f4f6;
            padding: 0.6mm 2mm;
            border-radius: 0.8mm;
            border: 0.5px solid #e5e7eb;
            white-space: nowrap;
        }
        .exp-company {
            font-size: 9.5pt;
            color: #3b82f6;
            font-weight: 600;
            margin-bottom: 1.5mm;
        }
        .exp-details {
            list-style: none;
            font-size: 8.5pt;
            color: #374151;
            line-height: 1.5;
        }
        .exp-details li {
            padding-left: 4mm;
            position: relative;
        }
        .exp-details li::before {
            content: '›';
            position: absolute;
            left: 0;
            color: #3b82f6;
            font-weight: 700;
        }

        /* Bottom row */
        .bottom-row {
            display: flex;
            gap: 5mm;
            margin-top: 1mm;
        }
        .bottom-col { flex: 1; }

        .edu-card {
            margin-bottom: 3mm;
        }
        .edu-header {
            display: flex;
            justify-content: space-between;
            align-items: baseline;
        }
        .edu-degree {
            font-size: 9.5pt;
            font-weight: 700;
            color: #111827;
        }
        .edu-date {
            /* Styles shared with .exp-date */
        }
        .edu-school {
            font-size: 8.5pt;
            color: #3b82f6;
            font-weight: 500;
        }

        .interest-row {
            display: flex;
            align-items: flex-start;
            gap: 1.5mm;
            margin-bottom: 1.5mm;
            font-size: 7.5pt;
            color: #374151;
            line-height: 1.3;
        }
        .interest-icon {
            font-size: 9.5pt;
            flex-shrink: 0;
            width: 5mm;
            text-align: center;
        }
    </style>
</head>
<body>
    <!-- Print Controls -->
    <div class="no-print" style="position:fixed; top:1rem; right:1rem; display:flex; gap:0.5rem; z-index:100;">
        <button onclick="window.print()" style="background:#3b82f6; color:white; border:none; padding:0.6rem 1.2rem; border-radius:0.5rem; font-weight:700; cursor:pointer; font-size:0.85rem;">
            🖨️ Imprimer / PDF
        </button>
        <a href="/" style="background:#374151; color:white; border:none; padding:0.6rem 1.2rem; border-radius:0.5rem; font-weight:700; cursor:pointer; text-decoration:none; font-size:0.85rem;">
            ⬅️ Retour
        </a>
    </div>

    <!-- A4 Page -->
    <div class="page">
        
        <!-- ═══ LEFT SIDEBAR ═══ -->
        <div class="sidebar">
            
            <div class="photo-wrapper">
                <img src={personalInfo.photo} alt="Jonathan GATARD" class="photo" />
            </div>

            <div>
                <div class="sidebar-name">{personalInfo.name}</div>
                <div class="sidebar-title">{personalInfo.title}</div>
            </div>

            <!-- Contact -->
            <div>
                <div class="section-title">Contact</div>
                <ul class="contact-list">
                    <li><span class="icon">📧</span> <span>{personalInfo.email}</span></li>
                    <li><span class="icon">📱</span> <span>{personalInfo.phone}</span></li>
                    <li><span class="icon">🌐</span> <span>{personalInfo.website}</span></li>
                    <li><span class="icon">💼</span> <span>{personalInfo.linkedin}</span></li>
                    <li><span class="icon">🚗</span> <span style="color:#93c5fd">{personalInfo.misc}</span></li>
                </ul>
            </div>

            <!-- Compétences -->
            <div>
                <div class="section-title">Compétences</div>
                <div class="skill-group">
                    <h3>Mainframe</h3>
                    <div class="skill-tags">
                        {skills.mainframe.map(s => <span class="skill-tag">{s}</span>)}
                    </div>
                </div>
                <div class="skill-group">
                    <h3>Open</h3>
                    <div class="skill-tags">
                        {skills.open.map(s => <span class="skill-tag">{s}</span>)}
                    </div>
                </div>
                <div class="skill-group">
                    <h3>Méthodologie</h3>
                    <div class="skill-tags">
                        {skills.methodology.map(s => <span class="skill-tag">{s}</span>)}
                    </div>
                </div>
            </div>

            <!-- Langues -->
            <div style="margin-top:auto;">
                <div class="section-title">Langues</div>
                {languages.map(l => (
                    <div class="lang-row">
                        <span>{l.name}</span>
                        <div class="lang-bar-bg">
                            <div class="lang-bar-fill" style={`width:${l.pct}%`}></div>
                        </div>
                    </div>
                ))}
            </div>
        </div>

        <!-- ═══ RIGHT MAIN ═══ -->
        <div class="main">
            
            <!-- Summary -->
            <div class="summary-box">
                Ma double compétence en <strong>Mainframe</strong> et <strong>Open</strong> me permet de moderniser les infrastructures historiques en y intégrant les meilleures pratiques d'automatisation et d'agilité.
            </div>

            <!-- Expériences -->
            <div>
                <div class="main-section-title"><span class="accent"></span> Expériences Professionnelles</div>
            </div>
            
            {experiences.map(exp => (
                <div class="exp-card">
                    <div class="exp-header">
                        <span class="exp-role">{exp.role}</span>
                        <span class="exp-date">{exp.date}</span>
                    </div>
                    <div class="exp-company">{exp.company}</div>
                    <ul class="exp-details">
                        {(exp.details || []).map(d => <li>{d}</li>)}
                    </ul>
                </div>
            ))}

            <!-- Formation -->
            <div>
                <div class="main-section-title"><span class="accent"></span> Formations</div>
            </div>
            <div style="margin-top:1mm;">
                {education.map(edu => (
                    <div class="edu-card">
                        <div class="edu-header">
                            <span class="edu-degree">{edu.degree}</span>
                            <span class="edu-date">{edu.date}</span>
                        </div>
                        <div class="edu-school">{edu.school}</div>
                        {edu.details && (
                            <ul class="exp-details" style="font-size: 7pt; margin-top: 0.5mm;">
                                {edu.details.map(d => <li>{d}</li>)}
                            </ul>
                        )}
                    </div>
                ))}
            </div>

            <!-- Projets & Passions -->
            <div>
                <div class="main-section-title"><span class="accent"></span> Projets & Passions</div>
            </div>
            <div class="bottom-row" style="margin-top:1mm; display: grid; grid-template-columns: 1fr 1fr; gap: 0 4mm;">
                {interests.map(i => (
                    <div class="interest-row">
                        <span class="interest-icon">{i.icon}</span>
                        <span>{i.text}</span>
                    </div>
                ))}
            </div>

        </div>
    </div>
</body>
</html>
